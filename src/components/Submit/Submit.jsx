import React from "react";

function Submit(props) {
  return (
     
      <img
        src={props.imageBtn}
        alt="SubmitButton"
        className="SubmitButton"
      >
      </img>
  );
}

export default Submit;
